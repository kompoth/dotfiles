# make sxhkd reload its configuration files
super + Escape
  pkill -USR1 -x sxhkd

# program launcher
super + d
  ~/dotfiles/scripts/dmenu_run_history -b -nf '#000000' -nb '#d0d0d0' -sf '#d0d0d0' -sb '#000000' -fn 'Iosevka:size=14'

# screenshot with maim
super + Print
  maim -so ~/screenshots/$(date +%F-%T).png

# show indicators
super + {t,b}
  ~/dotfiles/scripts/uinfo -{t,b}

# volume control
XF86Audio{LowerVolume,RaiseVolume,Mute}

  amixer set Master {5%-,5%+,toggle} && ~/dotfiles/scripts/uinfo -v

# backlight control
XF86MonBrightness{Down,Up}
  brightnessctl s {50-,+50} && ~/dotfiles/scripts/uinfo -bl
